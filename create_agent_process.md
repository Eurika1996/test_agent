# 测试 Agent 搭建路线图与架构说明

---

## 1. 搭建什么测试类的 Agent

| 编号 | Agent 名称 | 核心功能 | 状态 |
|------|-----------|---------|------|
| 1 | 需求文档评审 | 分析需求文档，识别完整性/不明确性/矛盾性等问题 | ✅ 已完成 |
| 2 | 生成测试用例 | 根据需求文档自动生成功能测试用例 | ⬜ 待开发 |
| 3 | 生成测试数据 | 自动生成覆盖各种边界条件的测试数据 | ⬜ 待开发 |
| 4 | 执行测试用例（UI） | 自动化执行 UI 测试（如 Playwright / Selenium） | ⬜ 待开发 |
| 5 | 接口文档评审 | 评审接口文档（OpenAPI/Swagger），识别缺失字段、错误码等 | ⬜ 待开发 |
| 6 | 生成接口测试用例 | 根据接口文档自动生成接口测试用例（如 pytest + requests） | ⬜ 待开发 |
| 7 | 执行接口测试用例 | 批量运行接口测试，收集结果 | ⬜ 待开发 |
| 8 | 生成测试报告（UI + 接口） | 汇总测试结果，生成可视化 HTML 报告 | ⬜ 待开发 |
| 9 | Agent Platform（智能体平台） | 统一入口，集成所有 Agent，可扩展新 Agent | ⬜ 待开发 |

---

## 2. 从 0 到 1 搭建每个 Agent

### 2.1 需求文档评审 Agent（✅ 已完成）
**prompt**：
我想从0-1搭建一个需求文档评审agent，要求能根据我提供的需求文档，找出问题，然后根据这些问题生成一个markddown文档，文档中包含问题的描述、改进建议、问题的严重程度（如P1、P2、P3等）。


**文件位置**：`c:\Users\Donot\Desktop\test_agent`

**运行方式**：
```bash
# 方式1：命令行
python main.py examples/sample_requirements.md

# 方式2：Web 界面
python app.py
# 浏览器打开 http://127.0.0.1:5000
```

**项目结构**：
```
test_agent/
├── main.py                    # 命令行入口
├── app.py                     # Web 界面入口（Flask）
├── requirements.txt           # Python 依赖
├── .env                       # API Key 配置
├── src/
│   ├── document_reader.py     # 文档读取模块（.md/.txt/.docx/.pdf）
│   ├── llm_client.py          # LLM API 客户端（兼容 OpenAI 接口）
│   ├── reviewer.py            # 评审核心逻辑（系统提示词 + JSON 解析）
│   └── report_generator.py    # 报告生成（Markdown + HTML）
├── examples/
│   └── sample_requirements.md # 示例需求文档
├── uploads/                   # 用户上传的文档（运行时）
└── outputs/                   # 评审报告输出（运行时）
```

**整体架构**：
```
┌─────────────────────────────────────────────────────────────────────┐
│                      🌐 网页界面 (app.py)                            │
│   ┌─────────────┐   ┌─────────────┐   ┌────────────────────┐       │
│   │ 文件上传表单 │ → │ 进度条/日志 │ → │ HTML 报告展示      │       │
│   └─────────────┘   └─────────────┘   └────────────────────┘       │
└──────────────────────────────────────┬──────────────────────────────┘
                                       │ HTTP API
                                       ▼
┌─────────────────────────────────────────────────────────────────────┐
│                 📖 文档读取 (document_reader.py)                    │
│   .md / .txt / .docx / .pdf  →  纯文本字符串                       │
└──────────────────────────────────────┬──────────────────────────────┘
                                       │
                                       ▼
┌─────────────────────────────────────────────────────────────────────┐
│                 📦 智能分块 (chunk_text)                            │
│   超长文档按段落切分为 4000 字符的小块，避免超出 LLM 上下文限制     │
└──────────────────────────────────────┬──────────────────────────────┘
                                       │
                                       ▼
┌─────────────────────────────────────────────────────────────────────┐
│                 🤖 LLM 评审核心 (reviewer.py)                       │
│   ┌──────────────────────────────────────────────────────────┐    │
│   │ 系统提示词 (System Prompt)                               │    │
│   │  "你是资深需求分析师，按5个维度评审..."                   │    │
│   │   1. 完整性   2. 不明确性   3. 矛盾性                    │    │
│   │   4. 不可实现性   5. 可测试性                             │    │
│   │  输出格式：JSON 数组，每个问题包含类型/级别/描述/建议      │    │
│   └──────────────────────────────────┬───────────────────────┘    │
│                                      │ 发送给 LLM                   │
│   ┌──────────────────────────────────▼───────────────────────┐    │
│   │  LLM 客户端 (llm_client.py)                              │    │
│   │  → 兼容 OpenAI 接口（DeepSeek/通义千问/SiliconFlow）     │    │
│   │  → 自动重试 3 次                                         │    │
│   └──────────────────────────────────┬───────────────────────┘    │
│                                      │ 返回 JSON 问题列表          │
│   ┌──────────────────────────────────▼───────────────────────┐    │
│   │ JSON 解析器                                               │    │
│   │  把 LLM 输出的 JSON 数组 → 结构化问题对象列表             │    │
│   └──────────────────────────────────────────────────────────┘    │
└──────────────────────────────────────┬──────────────────────────────┘
                                       │ 结构化问题列表
                                       ▼
┌─────────────────────────────────────────────────────────────────────┐
│                 📄 报告生成 (report_generator.py)                   │
│   按严重级别（P0/P1/P2/P3）+ 按类型分类 → Markdown + HTML           │
└─────────────────────────────────────────────────────────────────────┘
```

**核心设计要点**：

1. **Prompt Engineering**：通过精心设计的系统提示词，让 LLM 以固定 JSON 格式输出，实现"非结构化文档 → 结构化问题"的转换
2. **多服务商兼容**：只对接 OpenAI 风格的 `/chat/completions` 接口，改 `.env` 配置即可切换服务商（当前使用阿里云 DashScope / 通义千问）
3. **文档分块**：按段落切分文档，每块独立评审后合并去重，避免 LLM 上下文超限
4. **双入口**：命令行 `main.py` 适合脚本自动化，Web 界面 `app.py` 适合交互式使用

**已配置的 API Key**：
```
OPENAI_API_KEY=sk-59457476cc18447da793a7a2ebd98a37      # 阿里云 DashScope
OPENAI_API_BASE=https://dashscope.aliyuncs.com/compatible-mode/v1
OPENAI_MODEL=qwen-plus
LLM_PROVIDER=DashScope
```

---

### 2.2 生成测试用例 Agent（⬜ 待开发）

**预留设计思路**：
- 输入：需求文档 + 上一步生成的评审意见（可选）
- 输出：结构化测试用例（测试场景、前置条件、步骤、预期结果、优先级）
- 可导出为 Excel / Markdown / 测试管理工具（如 TestLink、XMeter）格式

**核心复用模块**：`document_reader.py` + `llm_client.py`（无需重写）
**需新增模块**：`test_case_generator.py`（测试用例生成逻辑 + 系统提示词）

---

## 3. Agent 之间的协作关系

```
需求文档  ───▶  [需求文档评审 Agent]  ───▶  评审报告.md
   │                                            │
   │                                            ▼
   └─────▶  [生成测试用例 Agent]  ──────▶  测试用例.md
                    │
                    ▼
            [生成测试数据 Agent]  ─────▶  test_data.json
                    │
                    ▼
    ┌─────────────────────────────────────┐
    │   [执行测试用例 Agent]               │
    │    - UI 测试（Playwright/Selenium） │
    │    - 接口测试（pytest + requests）  │
    └──────────────┬──────────────────────┘
                   │
                   ▼
            [测试报告 Agent]  ─────▶  test_report.html
                    │
                    ▼
        ┌────────────────────────┐
        │   Agent Platform       │
        │   统一入口/任务调度      │
        └────────────────────────┘
```

---

## 4. 快速开始

```bash
# 进入项目目录
cd c:\Users\Donot\Desktop\test_agent

# 启动 Web 界面（推荐）
python app.py
# 浏览器访问 http://127.0.0.1:5000

# 或使用命令行
python main.py examples/sample_requirements.md

# 如需切换 LLM 服务商，修改 .env 文件
```

---

*本文档随 Agent 的开发持续更新*
